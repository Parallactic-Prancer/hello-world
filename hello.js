let name = process.argv[2];
if (!name) {
    name = 'Stranger';
}
console.log(`Hello ${name}. This is the MASTER BRANCH`);
console.log(`See you later, ${name}.`);
console.log(`okay... Bye!!`);



console.log(`Here is some other stuff that was written in the testing branch and should always be present here from now on...`);
console.log('[testing] first commit made');
